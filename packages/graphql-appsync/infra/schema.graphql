
type Mutation {
  putUser(id: ID!, name: String!, age: Int!, city: String!): User
}

type User {
  id: ID!
  name: String
  age: Int,
  city: String
}

type Query {
  getUser(id: ID!): User
}

schema {
  query: Query
  mutation: Mutation
}