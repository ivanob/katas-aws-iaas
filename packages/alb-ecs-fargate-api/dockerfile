# Build stage
FROM rust:latest as builder
WORKDIR /app
COPY Cargo.toml Cargo.lock ./
COPY src ./src
RUN cargo build --release

# Runtime stage - NO NGINX NEEDED!
FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/kata1-api .
EXPOSE 80
CMD ["./kata1-api"]