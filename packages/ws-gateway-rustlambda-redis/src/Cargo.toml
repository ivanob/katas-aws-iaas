[package]
name = "lambda-handle-game-actions"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_http = "1.0.0"
lambda_runtime = "0.13"
redis = "1.0.3"
serde_json = "1.0"
aws-sdk-apigatewaymanagement = "1.93.0"
aws-config = "1.0"

tokio = { version = "1", features = ["macros"] }
aws-smithy-types = "1.4.4"
